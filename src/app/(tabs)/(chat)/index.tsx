import { View, Text } from 'react-native'
import React from 'react'
import { defaultStyles } from '../../../styles'

const ChatScreen = () => {
  return (
    <View style={defaultStyles.container}>
      <Text style={defaultStyles.text}>ChatScreen</Text>
    </View>
  )
}

export default ChatScreen